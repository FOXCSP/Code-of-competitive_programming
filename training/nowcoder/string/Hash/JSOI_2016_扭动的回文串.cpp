#include <bits/stdc++.h>

using namespace std;

const int N = 100010;
void solve() {
    int n; cin >> n;
    string a, b; cin >> a >> b;
    for(int i = 1; i <= n; i ++) {
        // yi i 为回文中心
        for(int i = 1; i <= )
    }
}


int main() {
    ios::sync_with_stdio(false), cin.tie(nullptr);
    solve();
    return 0;
}