#include <bits/stdc++.h>
#define fi first
#define se second
#define vec vector
#define P(T) pair<T,T>
#define SZ(T) (int)(T.size())
#define lp (p<<1)
#define rp (p<<1|1)
using namespace std;
typedef long long ll;
const ll MOD = 1e9+7;
const int N = 100010;

void solve(int CASE = 1) {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int _;
    cin >> _;

    for (int i = 1; i < _; i++) {
        solve(i);
    }

    return 0;
}

